import java.time.LocalDate;

class curdate {
    public static void main(String[] args) {
        // CURDATE() in Java - Returns current date
        LocalDate today = LocalDate.now();
        System.out.println("CURDATE(): " + today);
    }
}